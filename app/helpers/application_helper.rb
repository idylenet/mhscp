# Methods added to this helper will be available to all templates in the application.
module ApplicationHelper

  # will render a fully built indicator html chunk
  # indicator functionality includes unlimited granularity of color gradation
  # arbitrary tick mark indication
  def build_indicator(numRows, colorSettings, stepping)
    
  end
  
  def title(title)
    @title = title
  end

end
